#include <bits/stdc++.h>
#include <stdio.h>

using namespace std;

string numWheels;
string make;
string model;
Function toString()->returns "[make -> model]"

    class Vehicle
{
public:
    int numWheels;
    string make;
    string model;
    string toString()
    {
        return make + " ->" + model;
    }

} int main()
{
}